# Replaceable public assets

Add these files before publishing your own portfolio:

- `cv.pdf` — the public CV downloaded from the header.
- `og.png` — the 1536×1024 social-sharing image referenced by the metadata.

Never commit private identity documents, unpublished certificates, passwords,
API tokens, or an internal employer document.
